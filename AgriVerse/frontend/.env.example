# Leave blank while developing locally - the Vite dev server proxies
# /api requests to http://localhost:5000 automatically (see vite.config.js).
# Set this to your deployed backend URL when you build for production,
# e.g. VITE_API_BASE_URL=https://api.your-domain.com
VITE_API_BASE_URL=
